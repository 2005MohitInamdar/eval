import { DragnDrop } from './dragn-drop';

describe('DragnDrop', () => {
  it('should create an instance', () => {
    const directive = new DragnDrop();
    expect(directive).toBeTruthy();
  });
});
